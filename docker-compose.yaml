version: '3.8'
services:
  redis:
    image: redis:5
    container_name: caching-redis
    hostname: redis
    ports:
      - 6379:6379
    restart: always
    networks:
      - dinamicka
  api:
    build:
      context: .
    env_file:
      - .env
    image: caching-api
    container_name: caching-api
    hostname: api
    ports:
    - 5000:5000
    volumes:
    - ./public:/public
    restart: always
    depends_on:
      - redis
    networks:
      - dinamicka
networks:
  dinamicka:
    driver: bridge

